(function ($, Drupal) {
  'use strict';

  Drupal.behaviors.designTheme = {
    attach: function (context, settings) {
      // Custom JavaScript for the Design theme
      console.log('Design theme JS loaded');
    }
  };

})(jQuery, Drupal);
